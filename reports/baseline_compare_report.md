# Baseline Comparison Report

- Status: execution unavailable in current workspace
- Intended command: `python -m evaluation.baseline_compare --dataset evaluation/sample_dataset.json --report reports/baseline_compare_report.md`
- Lightweight model status: `unavailable (fallback)`
- Reason: `python` is not installed in the current shell, `py -0` reports `No Installed Pythons Found!`, and Docker service could not be started for container-based execution.

실제 실행 결과 표는 Python 또는 Docker 실행 환경이 준비된 뒤 위 명령으로 다시 생성해야 합니다.
